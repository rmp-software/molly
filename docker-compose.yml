services:
  db:
    image: postgres:16
    ports: ["5432:5432"]
    environment:
      POSTGRES_USER: molly
      POSTGRES_PASSWORD: molly
      POSTGRES_DB: molly
    volumes: ["postgres-data:/var/lib/postgresql/data"]
volumes:
  postgres-data:
